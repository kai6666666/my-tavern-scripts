# 如何修改骰子表格而不丢失脚本功能

## 总原则

- 最稳妥做法：保留默认表名和关键列名，只增删非关键列。
- 如果大幅改名，请在“设置 → 骰子系统预设 → 仪表盘预设”里配置表名和列名映射。
- 属性、关系、资源这类键值对，推荐统一写成 `名称:数值; 名称:数值`  `角色名:关系词; 角色名:关系词`这种格式。
- 内置数据验证和部分下拉/赠与功能仍依赖默认表名；想完全保留功能，建议保留 `主角信息`、`重要角色表`、`物品表`、`装备表` 等默认名。

## 功能与模板要求

| 功能 | 模板需要满足的条件 |
| --- | --- |
| 可视化表格编辑 | 任意表都能显示。建议第 1 列为 `row_id`，第 2 列为名称/标题，方便卡片标题、跳转、锁定和差异对比。 |
| 行动选项按钮 | 表名含地点/人物/角色/物品/装备/技能/任务/势力等关键词时会有默认按钮。若有列名包含 `交互`，内容可写 `交谈,观察,离开`，会追加成按钮。 |
| 选项表 | 表名包含 `选项`。除第 1 列外的非空单元格会变成可点击选项。默认验证期望选项表最多 1 行。 |
| 普通检定/对抗检定 | 需要能找到角色名列和属性列。主角表名建议含 `主角`；NPC 表建议使用 `重要角色表` 或 `重要人物表`。属性列名需包含 `属性`，推荐 `基础属性`、`特有属性`。 |
| 属性生成/清空/自动改值 | 同上。生成属性会写入 `基础属性`；若存在 `特有属性` 或 `特别属性` 列，会把特有属性分开写入。 |
| 属性预设 | 任意表格模板 note 中只要有闭合的 `<属性规则>...</属性规则>`，切换属性预设时就会同步更新该段。推荐放在主角表和重要角色表的 note 中。 |
| 检定建议表 | 表名包含 `检定建议`。需要 `展示文本` 列和 `骰子命令` 列；找不到时默认用第 2、3 列。note 中需要 `<检定规则>...</检定规则>` 才能跟随检定预设同步。 |
| 人物关系图 | 默认直接打开关系图时，角色表需要名称列，并且有列名包含 `人际关系`。值推荐 `御坂美琴:同学; 白井黑子:朋友`，也支持 `御坂美琴(同学)`。可选 `在场状态` 列用于筛选。 |
| 主角关系 | 主角表若要参与关系图，表名需为 `主角信息`，并有 `人际关系` 列。 |
| 仪表盘 | 默认识别下方“仪表盘默认识别规则”。若改成恋爱对象表、装扮表、备忘录等，请用仪表盘预设映射。 |
| 地图模块 | 需要地点表和地点名列；推荐保留 `世界地图点.详细地点`。地图元素表名需含 `地图元素`/`元素表`/`地图要素`/`机关`/`线索`，并有 `元素名称`、`元素类型`、`所在地点` 等列。所有“所在地点”应填写世界地图点里的详细地点名。 |
| 物品栏 | 需要能被识别为背包/物品表。默认 `物品表` 最完整；关键列为 `物品名称`、`类型`、`数量`、`品质`、`标签`、`效果`、`描述`。 |
| 骰子商店 | 奖励默认跟随仪表盘预设写入物品区或装备区解析到的表；自定义卡池也可用 `targetTable` 固定到指定表。物品型至少要能识别名称、类型、数量；装备型至少要能识别名称、类型、状态。基础列名不一致时用 `targetColumns`，额外必填列用 `customFields`。商店物品的 `标签`/`效果`/`描述` 三列独立解析：`标签` 按“标签/标记/词条”识别，`效果` 按“效果/作用/能力/特效”识别，`描述` 按“描述/说明/用途/备注”识别；精确列名优先于别名，避免“效果”列抢占“描述”。 |
| 装备展示 | 装备表关键列为 `装备名称`、`类型`、`部位`、`状态`、`品质`、`标签`、`效果`、`描述`。状态写 `已装备`/`装备中`/`是` 等会被仪表盘识别为当前装备。 |
| 头像管理 | 依赖角色名称。角色表名和名称列能被仪表盘或关系图识别时，头像会跟随角色名显示。 |
| 数据验证 | 内置验证主要针对默认表名和默认列名，例如 `主角信息.基础属性`、`重要人物表.人际关系`、`世界地图点.详细地点`。重命名后建议同步修改验证规则。 |

## 模板检验预设的分层

“检验表格模板”不是要求每张默认表都必须存在，也不是把整份默认模板当成硬性最低门槛。内置模板检验预设只检查骰子系统会特殊读取、写入或同步的核心表和关键列，并按影响分层：

- `error`：缺失后核心交互或写入会失败，例如角色名列、基础属性/NPC 特有属性列、抽卡写入物品/装备所需列。
- `warning`：功能会降级或部分模块不可用，例如地点层级、角色/地图元素的所在地点。
- `info`：辅助体验或提示词结构建议，例如 note、DDL、排序辅助列。

`纪要表`、`势力` 这类默认模板中很有用、但脚本当前只按普通表展示或普通交互处理的表，不在内置模板检验预设里。自定义世界观如果需要强制保留它们，可以新建模板检验预设，把对应表加入 `template`，并用 `requirementLevels` 指定严重程度。

模板检验预设的 `template` 只需要列出要检查的业务字段；默认内置预设不会把完整表格模板塞进去，也不会把 `row_id` 当作必须字段。`row_id` 仍然是完整表格模板里的推荐结构，但它不是骰子系统功能判断的核心列。

## 仪表盘默认识别规则

| 仪表盘区域 | 默认识别的表名关键词 | 关键列关键词 |
| --- | --- | --- |
| 全局状态 | `全局数据表`、`全局数据`、`全局` | `当前详细地点`、`当前次要地区`、`当前时间` |
| 主角 | `主角信息`、`主角`、`玩家`、`角色信息`、`user`、`<user>` | `姓名/名称`、`状态`、`位置/所在地`、`基础属性/属性`、`资源/金钱` |
| 地点 | `世界地图点`、`地图点`、`地图`、`地点`、`场景`、`区域` | `详细地点/地点名/名称`、`环境描述/描述` |
| NPC/角色 | `重要角色表`、`重要人物表`、`NPC`、`人物`、`角色`、`伙伴`、`队友` | `姓名/名称`、`状态`、`位置/所在地点`、`在场状态` |
| 任务 | `任务表`、`任务`、`事项`、`目标`、`待办`、`委托` | `名称`、`类型`、`进度`、`状态`、`优先级` |
| 物品 | `物品`、`背包`、`道具`、`库存`、`持有物品表` | `物品名称/名称`、`类型`、`数量` |
| 装备 | `装备表`、`装备`、`武器`、`防具`、`法宝` | `装备名称/名称`、`类型`、`部位/位置`、`状态` |

## 默认校验关注的表与列

- `全局数据表`：至少保留 `当前详细地点`、`当前次要地区`、`当前主要地区`、`当前时间`。
- `主角信息`：默认校验关注 `姓名`、`所在地点`、`基础属性`、`特有属性`。`自身状态`、`资源数据` 对展示和世界状态有用，但不作为默认校验要求。
- `重要角色表`：默认校验关注 `姓名`、`基础属性`、`特有属性`、`所在地点`、`在场状态`、`人际关系`。`交互选项` 对行动按钮有用，但不作为默认校验要求。
- `世界地图点`：默认校验关注 `详细地点`、`次要地区`、`主要地区`。`地点类型`、`环境描述`、`重要度`、`探索状态` 对展示有用，但不作为默认校验要求。
- `地图元素表`：默认校验关注 `元素名称`、`所在地点`。`元素类型`、`元素描述`、`状态`、`交互选项` 对展示和交互有用，但不作为默认校验要求。
- `物品表`：至少保留 `物品名称`、`类型`、`数量`、`品质`、`描述`。
- `装备表`：至少保留 `装备名称`、`类型`、`品质`、`状态`、`描述`，如需堆叠装备可加 `数量`。

`任务表` 和 `检定建议表` 默认不纳入模板检验预设。它们可以作为普通表或世界观辅助表保留；如果某个自定义玩法确实要强制检查它们，请在自定义模板检验预设里单独加入。

## 常用格式

```text
属性：力量:55; 敏捷:40; 侦查:70
资源：金币:250; 行动点:3
关系：御坂美琴:同学; 白井黑子:朋友,队友
交互选项：交谈,观察,赠送
骰子命令：检定 <user> 侦查 难度=困难
骰子命令：对抗 <user> 心理学 vs 守夜人 话术
骰子命令：无 / 必成 / 必败
```

## 改名时的简短建议

小改表头可以，只要保留关键词；大改表名时，请优先配置仪表盘预设。若你希望“下拉角色、赠与、内置验证、主角关系图”也完全无损，尽量不要改 `主角信息`、`重要角色表`、`物品表`、`装备表` 这些核心表名。
