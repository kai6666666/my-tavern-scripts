# UI/UX 评审基线

日期：2026-05-15

## 范围与方法

本基线对应第 0 批，只建立评审材料，不修改运行时代码。

检查范围：

- 主实现：`src/骰子系统/index.ts`
- 共享样式：`src/骰子系统/styles.ts`
- 教程遮罩：`src/骰子系统/tutorial.ts`
- 现有说明：`src/骰子系统/项目文档.md`

方法：

- 按 `impeccable` 的 product register 审视产品型工具界面。
- 静态抽样阅读仪表盘、设置、表格卡片、投骰面板、MVU、背包、抽卡和教程遮罩。
- 运行 `npx --yes impeccable detect --json --fast src/骰子系统` 做自动检测。
- 本轮未做 live browser overlay，因为骰子系统依赖 SillyTavern 注入环境，单独打开源码不能代表真实运行状态。

## 总体判断

当前 UI 功能完整，教程覆盖广，入口多，已经能支持复杂跑团工作流。主要问题不是功能缺失，而是视觉和交互语言过于分散：同类按钮、提示、弹窗、动效、强调方式在不同界面域里不完全一致，且大量模板字符串内联样式让后续统一成本变高。

默认方向应收敛为稳重工具感：保留主题能力，先让 `native`、`modern`、`dark` 在高频路径上稳定、清晰、可扫读。

本次重审的核心目标是统一美学系统。后续每批不能只修当前屏幕的局部观感，而要把当前界面域迁移到同一套组件、间距、层级、状态和动效规则里。专题界面可以保留内容气质，但不能重造控件语言。

## Nielsen 启发式基线

| # | Heuristic | Score | Key Issue |
|---|---|---:|---|
| 1 | 系统状态可见性 | 3 | 有 active 状态、toastr、历史和教程，但加载、保存、刷新状态不够统一。 |
| 2 | 符合用户现实语言 | 3 | 中文标签和跑团术语贴合场景，部分图标按钮仍依赖用户记忆。 |
| 3 | 用户控制与自由 | 3 | 多数弹窗有关闭、取消或返回，部分高密度菜单需要更明确的撤销和退出路径。 |
| 4 | 一致性与标准 | 2 | 按钮、提示、弹窗、强调和空状态存在多套视觉规则。 |
| 5 | 错误预防 | 2 | 锁定、确认、审核机制存在，但危险动作视觉优先级和确认文案不够统一。 |
| 6 | 识别优于记忆 | 3 | 教程系统覆盖广，标题和图标较完整，但高频界面可见选项偏多。 |
| 7 | 灵活性与效率 | 3 | 搜索、分页、快捷检定、预设和收藏支持效率用户，但入口密度较高。 |
| 8 | 美学与最小化 | 2 | 多主题和内联样式带来视觉噪音，部分装饰动效和厚侧边强调应移除。 |
| 9 | 错误恢复 | 2 | 局部支持撤销和错误提示，但错误、空值、失败保存的呈现不够系统。 |
| 10 | 帮助与文档 | 3 | 教程选择器覆盖主路径，这是明显优势；后续需要和 UI 改造同步维护。 |
| **Total** |  | **26/40** | 当前是功能强但视觉系统待统一的成熟工具雏形。 |

## 自动检测结果

命令：

```powershell
npx --yes impeccable detect --json --fast src/骰子系统
```

结果：25 条 warning。

| 类型 | 数量 | 代表位置 | 处理批次 |
|---|---:|---|---|
| 厚单侧强调线 | 10 | `index.ts:37661`、`index.ts:46053`、`styles.ts:7451` | 第 1 批 |
| 弹性或弹跳曲线 | 6 | `styles.ts:1155`、`styles.ts:3019`、`index.ts:62691` | 第 1 批 |
| 布局属性动画 | 9 | `index.ts:8054`、`styles.ts:1413`、`tutorial.ts:1867` | 第 1 批，教程项可延后到第 5 批 |

补充静态风险：

- 多处 `backdrop-filter` 用于弹窗和覆盖层，当前不一定都是问题，但后续需要避免变成默认玻璃拟态。
- 多处隐藏滚动条，适合沉浸面板，但在内容可滚动而用户无提示时会影响可发现性。
- 大量 HTML 内联样式集中在仪表盘、表格卡片和专题界面，后续统一视觉系统时应逐步抽到 class。

## 代表界面域评分

| 界面域 | 当前优势 | 主要风险 | 优先级 | 建议批次 |
|---|---|---|---|---|
| 主导航与面板壳层 | 入口完整，支持排序、隐藏、面板高度控制。 | 图标密度高，搜索、关闭、拖拽、视图切换挤在同一头部，移动端误触风险高。 | P1 | 第 2 批 |
| 仪表盘 | 能把角色、地点、NPC、物品、装备、任务集中呈现。 | 信息层级太密，内联小字较多，主要动作和辅助动作不够分离。 | P1 | 第 2 批 |
| 表格与卡片 | 搜索、分页、收藏、锁定、编辑和互动按钮完整。 | 卡片字段关系可扫性不稳定，空状态偏弱，编辑 affordance 依赖 hover/title。 | P1 | 第 3 批 |
| 设置 | 分组覆盖完整，能管理主题、布局、导航、预设和高级功能。 | 分组多且控件密，默认只展开外观，用户可能难以判断哪些设置影响当前问题。 | P2 | 第 3 或第 4 批 |
| 投骰与对抗 | 规则预设、角色快捷选择、历史和设置形成完整工作流。 | 表单字段多，规则切换后字段显隐和主动作优先级需要更稳定。 | P1 | 第 4 批 |
| MVU 面板 | 支持不同变量框架、数值模式、刷新、编辑和投骰入口。 | 树形层级和编辑状态需要更强的层级、空态、错误态表达。 | P2 | 第 3 批 |
| 背包与抽卡 | 视觉化程度高，有筛选、详情、赠与、兑换等专题流程。 | 专题界面自有视觉规则较多，容易和主工具语言脱节。 | P2 | 第 5 批 |
| 教程遮罩 | 覆盖面广，能降低复杂系统学习成本。 | 高亮动画包含布局属性动画，教程文案后续要随 UI 改造同步。 | P2 | 第 5 批 |

## 优先问题

### P1：组件语言不统一

同类按钮、图标按钮、提示、弹窗、菜单和卡片在不同界面域里出现不同形态。后续第 1 批应先建立基础组件规则，避免每个界面单独修。

验收标准：

- 主按钮、次按钮、图标按钮、危险按钮、禁用按钮状态清晰。
- focus、hover、active、disabled、error、warning、success 的变量和样式可复用。
- `native`、`modern`、`dark` 三个主题下对比度明确。
- 同一语义动作在仪表盘、表格、设置、投骰、MVU、背包、抽卡中使用同一种视觉表达。

### P1：自动检测明确命中的禁用样式

厚单侧强调线、弹性动效、布局属性动画都已经被检测到。它们是第 1 批的低争议改造项。

验收标准：

- 当前界面域内不再新增 `border-left` 或 `border-right` 大于 1px 的强调卡片。
- 弹性曲线替换为平滑 ease-out 曲线或移除。
- 当前界面域避免动画 `width`、`height`、`max-height`、`padding`、`margin`。

### P1：高频路径认知负担偏高

仪表盘、表格头部和投骰面板都承担大量操作。当前已经能用，但用户需要在很多小入口之间辨认主要动作。

验收标准：

- 每个界面域明确一个主要动作，其余动作按频率和风险降级。
- 图标按钮保留清晰 title 或 aria-label。
- 移动端操作目标不小于当前触屏可用标准，文字不重叠。

### P2：状态与反馈不成体系

加载、保存、刷新、失败、空状态、危险确认和撤销反馈散落在不同实现中。

验收标准：

- 当前界面域至少覆盖空状态、错误状态、保存成功、取消、关闭和危险确认。
- 错误文案告诉用户发生了什么，以及下一步能做什么。

### P2：主题个性与产品可用性冲突

强个性主题保留价值，但默认体验需要先稳定。后续应把主题分为基础验收主题和个性主题，避免所有主题同时阻塞主路径改造。

验收标准：

- 第 1 批先验证 `native`、`modern`、`dark`。
- 个性主题不得破坏按钮文字、active 文本、输入文本和 warning/error 文本的可读性。

## 分批验收顺序

1. 第 1 批：视觉系统与组件基础。先处理自动检测命中的禁用样式，并统一基础状态。
2. 第 2 批：主路径壳层。处理导航、面板头部、仪表盘层级和移动端误触，同时把主路径迁移到统一组件语言。
3. 第 3 批：数据浏览与编辑。处理表格卡片、菜单、收藏、变更审核、MVU，并统一编辑和状态反馈。
4. 第 4 批：投骰与规则工作流。处理普通检定、对抗、历史、规则预设，并统一表单和主要动作表达。
5. 第 5 批：专题界面。背包、抽卡、地图、关系图、头像、教程遮罩逐个验收，保留专题气质但服从统一控件规则。

## 第 0 批验收清单

- 根目录存在 `PRODUCT.md`，明确 product register、目标用户、使用场景和设计原则。
- 根目录存在 `DESIGN.md`，明确主题、排版、布局、组件状态、动效和禁用样式规则。
- 本文件记录代表界面评分、自动检测结果、优先问题和后续批次。
- 本批不修改 `index.ts`、`styles.ts`、`tutorial.ts` 的运行时代码。
- `pnpm build` 通过后，第 0 批可视为完成。

## 第 1 批执行记录

日期：2026-05-15

目标：建立视觉系统与组件基础，先清理自动检测命中的禁用样式，并为后续界面域迁移提供共享状态语法。

已完成：

- 在 `styles.ts` 中加入共享 motion、focus、disabled、warning/error/accent 边框 token。
- 为全局按钮基类补齐更稳定的 transition、focus ring 和 disabled 表达。
- 新增 `acu-inline-callout` / `acu-inline-callout-warning`，用于替代内联厚侧边提示块。
- 将厚单侧强调线改为完整边框或淡色背景，包括验证规则提示、模板检查卡、智能修复提示、MVU 数值项、调试堆栈和仪表盘任务项。
- 将弹性或弹跳曲线替换为平滑 ease-out，包括地图头像、地图地点图标、缩略图徽标、调试开关和冲突提示弹窗。
- 移除或降级布局属性动画，包括搜索框宽度动画、折叠区 max-height 动画、抽卡进度条 width 动画、教程高亮 width/height 动画。

验证：

- `npx --yes impeccable detect --json --fast src/骰子系统` 返回空数组。
- `git diff --check` 通过。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。

注意：

- `styles.ts` 中仍有一处 `border-left: 2px`，它是抽卡物品复选框勾选符号的笔画，不是卡片、列表项、提示或告警的侧边强调，因此不属于本次禁用样式。
- 本批没有改变 SillyTavern API、变量体系、表格数据结构、localStorage 键或主题 id。

## 第 2 批执行记录

日期：2026-05-15

目标：优化主路径壳层，把导航、面板头部和仪表盘迁移到更统一的产品型组件语言。

已完成：

- 为主导航按钮和固定动作按钮补齐 `type="button"`、`title`、`aria-label` 和当前态 `aria-pressed`。
- 为导航项增加显式 `NavigationItem` 类型，避免主路径渲染继续依赖隐式数组结构。
- 统一导航按钮、动作按钮、面板弹出、面板头部、仪表盘操作图标的 transition 与 focus 表达。
- 将主导航与面板头部中的 dashed 分隔、弹跳/放大反馈降级为更稳定的边框、阴影和轻微位移。
- 将仪表盘主角、地点、角色、物品、装备、任务区域迁移到共享的 section、heading、list、row 类名。
- 减少仪表盘关键结构的内联样式，为后续数据浏览、投骰、专题界面复用统一样式打基础。

验证：

- `git diff --check` 通过。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回空数组。

验收重点：

- 主导航按钮 hover、active、focus、审核警告态是否更一致，且不再明显弹跳。
- 面板打开关闭是否是轻微淡入/位移，不影响点击关闭与高度拖动。
- 仪表盘三列的标题、列表、操作图标是否更统一，移动端点击热区是否仍足够。
- 主角属性、地点、角色、物品、装备、任务的文字是否不重叠、不溢出。

注意：

- 本批不改导航排序存储、隐藏入口配置、面板高度存储、主题 id 或表格数据结构。
- 仪表盘内部仍保留少量动态内联样式，例如头像背景、进度条百分比和地图/图标内容，这是数据驱动样式，后续界面域处理时再拆。

## 第 3 批 A 执行记录

日期：2026-05-15

目标：先处理数据浏览与编辑的公共入口：表格卡片、单元格菜单、收藏夹卡片菜单和变更审核操作按钮。

已完成：

- 表格空状态从内联样式改为统一 `acu-empty-state`。
- 表格头部常用图标按钮补齐 `type="button"`、`aria-label` 和更明确的 title。
- 卡片、卡片行、卡片底部动作区、分页条从 dashed 分隔迁移到更稳定的 solid 分隔。
- 表格行动按钮、书签、分页按钮、卡片 hover 的动效改为明确属性动画，减少 `transition: all` 和放大反馈。
- 单元格菜单与收藏夹菜单项从 `div` 改为真正的 `button`，保留原有事件选择器。
- 单元格菜单新增统一 separator，并让删除项在收藏夹菜单中也使用危险色语义。
- 变更审核操作按钮与批量按钮迁移到主题变量背景/边框，减少深色主题下依赖白色半透明的情况。

验证：

- `git diff --check` 通过。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回空数组。

验收重点：

- 打开普通表格，列表视图/网格视图的卡片行、底部行动按钮、分页按钮是否更统一。
- 搜索不到或表为空时，空状态是否居中、清楚、不再像临时文字。
- 右键单元格菜单和收藏夹卡片菜单是否仍能编辑、复制、收藏、删除、关闭。
- 变更审核面板的接受/回滚/编辑按钮是否清楚，危险/成功语义是否可读。

注意：

- 本批不改表格数据结构、分页状态、搜索状态、收藏数据或锁定 API。
- MVU 面板深层编辑、完整审核信息结构和表格整体编辑弹窗将在后续小批次继续统一。

补充修正（2026-05-16）：

- 收藏夹头部的导入 / 导出按钮位于 `acu-fav-transfer-actions` 包裹层内，完整审核 / 数据验证头部的批量操作按钮位于 `acu-changes-batch-actions` 包裹层内，未命中第四批补上的直接子按钮 ghost 规则。
- 已将这两个包裹层内的按钮纳入统一头部图标按钮规则，默认透明背景、透明边框、固定 `34px` 触控框。
- 数据验证模式的筛选激活态改为只用强调色图标表达，默认不再出现额外按钮底框。
- 同步移除两个包裹层的内联 flex 样式，并为审核批量按钮补齐 `type="button"` 与 `aria-label`。
- 使用 Chrome DevTools Protocol 渲染收藏夹、完整审核、数据验证三种头部检查页，覆盖桌面 `1280x900` 与移动 `393x852`；7 个目标按钮均为 `34x34`、透明边框、透明背景，且无横向溢出或中文编码坏点。

## 第 3 批 B 执行记录

日期：2026-05-15

目标：统一 MVU 面板与编辑/审核弹窗的基础控件状态，先处理高频编辑路径，不改数据保存流程。

已完成：

- MVU 顶部刷新、关闭、数值模式按钮补齐 `type="button"`、`aria-label` 和模式 `aria-pressed`。
- MVU 数值模式层级开关补齐 `aria-pressed`，点击后同步 `data-visible`、`active` class 和无障碍标签。
- MVU 顶部按钮、层级开关、数值项、折叠头和骰子快捷图标迁移到明确属性动画，去掉这一区域的 `transition: all` 与 hover 放大。
- MVU 变量编辑弹窗、变更审核编辑弹窗、单字段编辑弹窗、整行对比编辑弹窗、通用单元格编辑弹窗的操作按钮补齐 `type="button"`。
- 编辑弹窗输入框、下拉框、文本域统一使用主题 focus ring，减少原有硬编码 outline。
- 变更审核对比弹窗中只读旧值与当前可编辑区域改为更克制的完整边框和主题 focus 表达。

验证：

- `git diff --check` 通过。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回空数组。

验收重点：

- 打开 MVU 面板，刷新、关闭、数值模式切换按钮是否更像同一套工具按钮。
- 数值模式中层级显示/隐藏按钮是否仍能正确切换，并且 hover 不再跳动或放大。
- 点击 MVU 数值、审核面板编辑、单字段编辑、整行编辑、普通单元格编辑时，取消/保存/恢复按钮是否样式一致且不误提交。
- 输入框、文本域、下拉框获得焦点时是否有清晰但不过重的主题描边。

注意：

- 本批不改 MVU/LWB/ERA 数据写入、审核快照、保存函数、localStorage 键或事件选择器。
- `styles.ts` 中其他专题界面仍保留一些旧动效，将在后续专题批次按界面域继续处理。

## 第 4 批 A 执行记录

日期：2026-05-15

目标：先统一普通检定与对抗检定主面板的控件基础，让投骰入口、预设、快捷选择和主动作按钮遵循同一套产品组件语言。

已完成：

- 普通检定面板顶部的对抗切换、历史、设置、关闭按钮补齐 `type="button"`、`aria-label` 和统一图标按钮 class。
- 对抗检定面板顶部的普通切换、历史、设置、关闭按钮补齐同样的按钮语义。
- 普通/对抗检定的预设按钮、角色快捷按钮、属性快捷按钮、生成/清空属性、重投、资源消耗按钮补齐 `type="button"`，避免表单内误触发提交。
- 投骰面板的顶部图标按钮、预设按钮、返回按钮、快捷属性按钮、主投骰按钮迁移到明确属性动画和统一 focus ring。
- 主投骰按钮文字色改为使用 accent 背景专用变量，修正浅色主题下可能出现的蓝底深字问题。
- 随机技能小按钮从虚线强调收敛为标准边框，减少与其他小图标按钮的视觉割裂。

验证：

- `git diff --check` 通过。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回空数组。

验收重点：

- 打开普通检定面板，顶部切换、历史、设置、关闭图标是否像同一组工具按钮。
- 选择检定规则、快捷角色、快捷属性时，hover/focus/active 是否稳定，不跳动、不放大。
- 点击“掷骰！”后，结果内的重投按钮和资源消耗按钮是否仍可正常点击，且不触发额外提交。
- 打开对抗检定面板，双方角色/属性快捷选择、规则预设、开始对抗按钮是否与普通检定保持一致。
- 在 `modern` 浅色主题下，主投骰按钮文字是否清楚可读。

注意：

- 本批不改骰子公式、判定算法、历史存储、预设数据结构、localStorage 键或输出模板。
- 检定历史弹窗、骰子设置弹窗和高级预设管理器仍作为第 4 批后续小批次处理。

## 第 4 批 B 执行记录

日期：2026-05-16

目标：统一检定历史弹窗，让普通检定入口和全局历史入口共享同一套列表、过滤、统计和详情展开视觉。

已完成：

- 新增精简的 `acu-history-*` 历史弹窗样式 capsule，覆盖头部、过滤区、统计卡、历史列表、详情块、底部操作区。
- 普通检定面板内的历史弹窗从大量内联样式迁移到共享类名。
- 全局检定历史弹窗同步迁移到同一套共享类名，保留复制详情、展开详情、筛选、搜索、清理历史事件。
- 历史列表项的普通/对抗标签、结果、骰值、效果状态、时间、展开按钮和复制按钮统一对齐与 focus 表达。
- 历史统计区域改为共享渲染 helper，普通入口和全局入口使用同一套三列统计卡与摘要行。
- 空历史状态改用已有 `.acu-empty-state`，不再为历史弹窗单独维护一套空状态样式。
- 关闭、清理历史、展开详情、复制详情按钮补齐 `type="button"` 与 `aria-label`。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- 使用 Chrome headless 渲染检定设置检查页，入口管理四行高度为 `48 / 48 / 48 / 49px`，确认疯狂模式不再形成异常大块。

验收重点：

- 从普通检定面板打开“检定历史”，搜索、状态筛选、统计范围切换是否正常。
- 从对抗检定或全局历史入口打开历史弹窗，列表样式是否与普通入口一致。
- 展开详情、复制详情、清理历史、关闭弹窗是否仍按原逻辑工作。
- 空历史时是否显示居中的空状态，不再像临时提示文字。
- 移动端下筛选控件、统计卡和列表项是否不挤压、不重叠。

注意：

- 本批不改历史数组、统计库、事件订阅、复制内容、清理历史逻辑或检定结果写入流程。
- 骰子设置弹窗和高级预设管理器仍作为第 4 批后续小批次处理。

补充修正（2026-05-16）：

- 根据后续验收反馈，检定历史筛选区改为一行紧凑工具栏，统计卡压缩为三列紧凑条，移动端统计摘要改为范围独占一行、数值四列网格，小屏退为两列。
- 检定设置弹窗移除误放的旧图标入口，保留主设置里的图标预设入口。
- 使用真实 Chrome DevTools Protocol 复核移动 `393x852` 与桌面 `1024x768`，历史筛选和统计卡 `rowSpread=0`，搜索框图标 padding 生效，检定设置中不再出现图标预设入口。
- 对移动端历史弹窗追加单独 CDP 截图与 DOM 指标复核，`statsOverflow=0`，中文文本无 `??` / `????` 乱码。
- `pnpm build`、`npx impeccable detect`、`git diff --check` 均通过；构建仍只有既有 bundle 体积 warning。

## 第 4 批 C 执行记录

日期：2026-05-16

目标：统一检定设置弹窗壳层，让它和投骰面板、历史弹窗共享稳重工具感，同时保持检定预设、属性预设、图标预设和疯狂模式入口的独立职责。

已完成：

- 检定设置弹窗移除壳层、头部、滚动体、分区、管理按钮、疯狂模式下拉的内联样式，迁移到 `acu-dice-settings-*` 类。
- 标题区改为统一图标标题与右侧教程/关闭按钮组合，关闭按钮补齐 `type="button"`、`aria-label` 和 `title`。
- 设置内容拆为“入口管理”和“结果显示”两个分区，用分隔线组织层级，不强行改成卡片网格。
- 管理按钮复用现有 `.acu-setting-action-btn`，只用小范围 modifier 控制当前弹窗宽度与密度。
- 检定设置弹窗的 scoped CSS 放在通用设置按钮规则之后，避免被 `.acu-edit-dialog` / `.acu-setting-action-btn` 后置规则撑宽。
- 收紧当前弹窗内的设置行距和分区间距，避免“疯狂模式”行被分区留白放大成异常大块。
- 保留疯狂模式下拉、隐藏输入栏结果、覆盖上一次结果、隐藏聊天记录结果的原事件与保存逻辑。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。

验收重点：

- 从普通检定面板打开设置，标题、教程、关闭按钮是否与历史弹窗气质一致。
- “检定预设 / 属性预设 / 图标预设 / 疯狂模式”四行是否对齐稳定。
- 点击三个“管理”入口是否仍进入原来的对应管理界面。
- 修改疯狂模式和三个结果显示开关后，保存行为是否与原来一致。
- 移动端下管理按钮和下拉框是否不挤压标签文字。

注意：

- 本批不改高级预设列表、预设编辑器 JSON 表单、属性预设管理器、疯狂模式配置结构或任何 storage key。
- 高级预设管理列表和编辑器仍作为第 4 批后续小批次处理。

## 第 4 批 D 执行记录

日期：2026-05-16

目标：统一检定预设管理列表，让预设条目、底部操作和管理弹窗壳层与前几批的稳重工具感保持一致。

已完成：

- 检定预设管理弹窗移除头部、提示、列表、底部按钮和文件输入的内联样式，迁移到 `acu-advanced-preset-*` 类。
- 预设列表项复用已有 `.acu-preset-*` 组件语义，补齐显示/隐藏、复制、编辑、导出、删除、拖拽手柄的按钮类型和可访问标签。
- 内置预设标记改为 `.acu-preset-badge` 小徽标，不再用临时括号文字和内联颜色。
- 空列表改用已有 `.acu-empty-state`，与历史弹窗等空状态保持一致。
- 底部“新建 / 导入 / 返回”改为共享 `.acu-dialog-btn` 按钮体系，并为窄屏增加换行和宽度约束。
- 移动端列表动作按钮收紧到 28px，长预设名保持省略，不挤压操作按钮。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- 按新增流程尝试 Codex 浏览器插件检查，但当前会话未发现 in-app browser 后端；已回退到本地 Chrome headless 检查页。
- Chrome headless 桌面检查：弹窗 `600x412`，列表项高度 `74/74/74`，长标题与动作按钮无重叠。
- Chrome headless 移动检查：弹窗 `370x443`，列表项高度 `72/72/72`，长标题与动作按钮无重叠，底部按钮换行正常。

验收重点：

- 从检定设置进入“检定预设管理”，标题、教程、关闭按钮是否与历史/设置弹窗一致。
- 内置预设徽标、显示/隐藏、复制、导出、自定义预设编辑/删除按钮是否位置清楚。
- 长预设名称和长描述是否省略而不是顶开按钮。
- 拖拽排序手柄在桌面端是否仍可用；移动端隐藏手柄后列表是否仍不挤压。
- 新建、导入、返回按钮是否仍触发原逻辑。

注意：

- 本批不改预设数据结构、导入导出 JSON、排序存储、内置预设内容或预设编辑器。
- 预设编辑器 JSON/条件分支表单可作为第 4 批后续小批次继续处理。

## 第 4 批 E 执行记录

日期：2026-05-16

目标：统一检定预设编辑器外壳与 JSONC 编辑区，让“新建/编辑检定预设”与预设管理列表、检定设置和历史弹窗保持同一套产品组件语言。

已完成：

- 检定预设编辑器移除弹窗壳层、头部、基础字段、JSONC 工具栏、配置格式区块和底部按钮的内联样式。
- 编辑器头部复用 `acu-advanced-preset-header`，关闭按钮补齐 `type="button"`、`aria-label` 和 `title`。
- 预设名称与描述整理为 `acu-advanced-preset-editor-fields` 两列字段，移动端自动收为单列。
- JSONC 区拆为标题说明、工具按钮和编辑 textarea，下载 AI 提示词、验证配置按钮接入共享 `.acu-dialog-btn`。
- JSONC textarea 改为专用 `.acu-advanced-preset-json-textarea`，保持等宽字体、可调整高度和稳定边框/focus。
- 配置格式区块改为 `.acu-advanced-preset-format-help`，与空状态/提示块一样使用主题变量。
- 保存/取消改为 `acu-advanced-preset-editor-footer`，窄屏下保存按钮独占一行，取消按钮保留清晰关闭路径。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- 使用 Chrome headless 渲染检定预设编辑器检查页，桌面尺寸下弹窗 `720x730`，无横向溢出，textarea `682x416`。
- 使用 Chrome headless 渲染移动尺寸检查页，弹窗 `370x719`，无横向溢出，textarea `340x312`，工具按钮、字段和底部按钮均未溢出。

验收重点：

- 从检定预设管理点击“新建检定预设”或编辑自定义预设，弹窗标题、教程和关闭按钮是否与其他弹窗一致。
- 名称、描述输入框在桌面和移动端是否不挤压、不丢失 focus 样式。
- “下载 AI 提示词”和“验证配置”按钮是否仍触发原逻辑。
- JSONC 编辑区是否仍能输入、滚动、调整高度，并保持可读。
- 保存、取消、点击遮罩关闭是否仍按原逻辑工作。

注意：

- 本批不改 JSONC 模板、预设解析校验、导入导出格式、保存逻辑、预设数据结构或 localStorage。
- 更进一步的结构化 outcomes/条件分支编辑器需要单独规划，避免把 JSON 协议编辑和表单编辑混在同一小批里。

## 第 4 批 F 执行记录

日期：2026-05-16

目标：统一属性预设管理与编辑体验，让属性预设和检定预设共享同一套弹窗、列表、JSON 编辑区、提示块和底部操作语言。

已完成：

- 属性预设管理弹窗移除头部、列表、底部按钮和文件输入的内联样式，复用统一弹窗壳层。
- 默认/内置标记改为 `.acu-preset-badge`，不再用临时括号文字和内联颜色。
- 启用开关、复制、编辑、导出、删除按钮补齐 `type="button"`、`title` 和 `aria-label`。
- 新建、导入、返回按钮接入共享 `.acu-dialog-btn`，移动端自动换行。
- 属性预设编辑器移除壳层、字段、JSON 工具栏、格式说明和底部按钮的内联样式。
- 属性预设名称与描述复用两列字段布局，移动端收为单列。
- JSON 编辑区复用统一 textarea 样式，并为属性预设保持更短的编辑高度。
- 属性预设格式说明移动到 JSON 编辑区上方，避免教程锚点落在长 textarea 下方。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- 使用 Chrome headless 渲染属性预设管理器检查页，桌面弹窗 `600x403`、移动弹窗 `370x441`，无横向溢出，列表文字与动作按钮无重叠。
- 使用 Chrome headless 渲染属性预设编辑器检查页，桌面弹窗 `720x674`、移动弹窗 `370x719`，无横向溢出；格式说明位于 textarea 前且可见。

验收重点：

- 从检定设置进入“属性预设”，默认预设、内置预设和自定义预设是否显示一致。
- 启用开关是否仍保持单选逻辑，关闭正在启用的预设是否回到默认规则。
- 复制、编辑、导出、删除、新建、导入、返回是否仍触发原逻辑。
- 新建或编辑属性预设时，名称、描述、JSON、格式说明、保存/取消是否不挤压、不重叠。
- 属性预设教程走到“格式说明”时，提示是否指向 JSON 编辑区上方的说明块。

注意：

- 本批不改属性预设结构、默认预设、内置预设、导入导出格式、保存逻辑、quickSelect 规则或 storage key。
- 属性预设的结构化表单编辑仍保持为 JSON 编辑模式，后续如需做可视化编辑器应单独规划。

## 第 4 批 G 执行记录

日期：2026-05-16

目标：统一交互规则预设管理与编辑体验，让交互规则预设接入前几批已经稳定下来的预设列表、JSON 编辑区、格式提示和底部操作语言。

已完成：

- 交互规则管理弹窗移除壳层、标题、列表、空状态、底部按钮和删除按钮的内联样式，复用统一预设弹窗类。
- 内置规则标记改为 `.acu-preset-badge`，和检定预设、属性预设保持一致。
- 启用开关、复制、编辑、导出、删除按钮补齐 `type="button"`、`title` 和 `aria-label`。
- 长规则名称、描述、关键词摘要和动作摘要保持省略，不再挤压右侧操作按钮。
- 交互规则编辑器移除字段、JSON 工具栏、textarea、格式说明和底部按钮的内联样式。
- JSON 配置区复用统一工具栏与 textarea 样式，并为交互规则保持比检定预设更短的编辑高度。
- “配置格式”说明统一放在 textarea 下方，编辑时先看到 JSONC 内容，再查看必要字段说明。
- 根据验收反馈，将检定预设、属性预设、交互规则编辑器里的“下载 AI 提示词 / 验证配置”统一移动到底部操作区。
- 桌面端底部按“下载 AI 提示词 / 验证配置 / 保存 / 取消”同排展示；移动端改为 2x2 等宽按钮，避免按钮大小参差。

验证：

- `git diff --check` 通过。
- `npx --yes impeccable detect --json --fast src/骰子系统` 通过，结果为空数组。
- `pnpm build` 通过，仍保留骰子系统 bundle 体积过大的既有 webpack performance warning。
- 使用 Chrome headless 渲染交互规则管理器检查页，桌面弹窗 `600x393`，移动弹窗 `370x477`，无横向溢出，列表文字与操作按钮无重叠。
- 使用 Chrome headless 渲染交互规则编辑器检查页，桌面弹窗 `720x652`，移动弹窗 `370x719`，无横向溢出；配置格式区块位于 textarea 下方且可见。
- 使用 Chrome headless 复查检定预设、属性预设、交互规则编辑器底部按钮，桌面同排无重叠；移动端工具按钮与提交按钮为 2x2 等宽排布。

验收重点：

- 从交互总览右上角齿轮或骰子系统预设进入“交互规则预设”，内置预设和自定义预设是否显示一致。
- 启用开关是否仍保持单选逻辑，关闭当前规则是否仍会禁用全部交互规则。
- 复制、编辑、导出、删除、新建、导入、返回是否仍触发原逻辑。
- 新建或编辑交互规则时，名称、描述、JSON、配置格式区块、保存/取消是否不挤压、不重叠。
- “下载 AI 提示词”和“验证配置”是否仍按原逻辑工作。
- 检定预设、属性预设、交互规则编辑器底部按钮顺序是否符合直觉，移动端四个按钮是否等宽整齐。

注意：

- 本批不改交互规则数据结构、默认内置规则、导入导出格式、保存逻辑、动作模板语法或 storage key。
- 交互规则仍保持 JSON 数组编辑模式；可视化规则组/动作编辑器可作为后续独立批次规划。

## 第 4 批 H 执行记录

日期：2026-05-16

目标：统一仪表盘预设管理与编辑体验，让仪表盘预设接入同一套预设列表、弹窗、JSON 编辑区、格式提示和底部操作语言。

已完成：

- 仪表盘预设管理弹窗移除壳层、头部、列表项、底部按钮和删除按钮的内联样式，复用统一预设弹窗类。
- 内置标记改为 `.acu-preset-badge`，和检定预设、属性预设、交互规则保持一致。
- 启用开关、复制、编辑、导出、删除按钮补齐 `type="button"`、`title` 和 `aria-label`。
- 长仪表盘名称、描述、区域摘要和关键词摘要保持省略，不再挤压右侧开关与图标按钮。
- 仪表盘预设编辑器移除字段、JSON 工具栏、textarea、格式说明和底部按钮的内联样式。
- “下载 AI 提示词 / 验证配置”移动到底部操作区，桌面端与保存/取消同排，移动端为 2x2 等宽按钮。
- 仪表盘格式说明移动到 textarea 上方，避免教程或视觉焦点落到长编辑框之后。
- 高级预设弹窗改为 `box-sizing: border-box`，让移动端宽高约束包含 padding 和 border，减少临界宽度溢出风险。

验证：

- 使用 Chrome headless 渲染仪表盘预设管理器检查页，桌面弹窗 `640x313`、移动弹窗 `370x398`，无横向溢出，列表文字与操作按钮无重叠。
- 使用 Chrome headless 渲染仪表盘预设编辑器检查页，桌面弹窗 `760x679`、移动弹窗 `370x719`，无横向溢出；格式说明位于 textarea 前且可见。
- 移动端仪表盘预设编辑器底部四个按钮为 2x2 等宽排布，每行按钮宽度差为 `0`。

验收重点：

- 从骰子系统预设或仪表盘齿轮进入“仪表盘预设”，内置预设和自定义预设是否显示一致。
- 启用开关是否仍保持单选逻辑，关闭正在启用的预设是否回到默认仪表盘。
- 复制、编辑、导出、删除、新建、导入、返回是否仍触发原逻辑。
- 新建或编辑仪表盘预设时，名称、描述、JSON、格式说明、保存/取消是否不挤压、不重叠。
- “下载 AI 提示词”和“验证配置”是否仍按原逻辑工作。

注意：

- 本批不改仪表盘预设数据结构、默认内置预设、导入导出格式、保存逻辑、区域渲染方式或 storage key。
- 仪表盘预设的可视化模块编辑仍未展开，后续若要替代 JSON 编辑模式应单独规划。

## 第 5 批 A 执行记录

日期：2026-05-16

目标：统一教程遮罩与教程弹窗的产品组件语言，让后续专题界面验收时教程层不再成为遮挡、错位或按钮不齐的风险源。

已完成：

- 教程弹窗改为稳定的 `box-sizing: border-box`，圆角、头部、正文、底部按钮与当前弹窗体系靠齐。
- 教程标题增加 `.acu-tutorial-title`，长标题允许安全换行，不挤压问号图标。
- 教程标题栏增加右上角关闭按钮，底部操作区收敛为“上一步 / 下一步”两列等宽单行布局。
- 教程按钮补齐 `type="button"`，增加图标、focus-visible、hover、disabled 和 primary 状态。
- 教程弹窗补齐 `role="dialog"`、`aria-modal`、`aria-labelledby`、`aria-describedby`，让教程层语义更明确。
- 高亮层继续只对 `transform` 和 `opacity` 做过渡，不对 `width`、`height` 等布局属性做动画。

验证：

- 使用 Chrome headless 渲染教程遮罩检查页，桌面弹窗底部两个按钮同排等宽，按钮文字无溢出。
- 使用 Chrome headless 渲染教程遮罩检查页，移动弹窗底部按钮为两列单行等宽，按钮文字无溢出。
- 检查页复测无教程层横向溢出，popover 右边界保持在移动视口内。

验收重点：

- 打开任意教程，标题、正文和进度是否可读。
- 桌面端底部两个教程按钮是否整齐，不再出现大块空白或按钮挤压。
- 移动端底部按钮是否为单行等宽排列，右上角关闭按钮是否可点且不挤压标题。
- 上一步、关闭、下一步/完成是否仍按原逻辑工作。

注意：

- 本批不改教程步骤文案、教程选择器、完成状态存储 key 或自动播放逻辑。
- 已经自动验收但尚未人工确认。

## 第 5 批 B 执行记录

日期：2026-05-16

目标：统一背包/物品栏主界面与物品详情的关键交互控件，保留物品栏专题视觉，但让移动端标题、动作按钮和关闭路径更稳定。

已完成：

- 物品卡片主按钮补齐 `type="button"` 和详情 `aria-label`。
- 物品栏搜索框补齐 `aria-label`，商店、打开表格、关闭按钮补齐 `type="button"` 与 `aria-label`。
- 物品详情关闭按钮补齐 `type="button"` 与 `aria-label`。
- 物品详情弹窗、描述块和元信息块改为 `box-sizing: border-box`，减少边框盒临界溢出风险。
- 移动端物品详情头部改为稳定网格：物品图标与标题信息同排，右上动作按钮另起一行，不再挤压物品名。
- 移动端物品详情底部动作区改为自适应网格，动作按钮统一触控高度和宽度，避免小按钮扎堆。

验证：

- 使用 Chrome headless 渲染物品栏检查页，移动端主界面根容器 `472x698`，无横向溢出，按钮文字无溢出。
- 使用 Chrome headless 渲染物品详情检查页，移动端详情弹窗 `444x385`，无横向溢出，标题不再被压成竖排。
- 移动端物品详情底部动作按钮为同排等宽 `131px`，高度 `38px`，按钮文字无溢出。
- 桌面端物品栏和详情页截图复查未发现明显错位。

验收重点：

- 从物品表或仪表盘进入物品栏，顶部搜索、教程、商店、打开表格、关闭是否仍可用。
- 物品卡片点击详情是否仍打开对应物品详情。
- 物品详情在手机宽度下，标题、赠与按钮、右侧工具按钮、元信息和描述是否不重叠。
- 分解碎片、使用、交给同伴等动作按钮是否仍按原逻辑触发。

注意：

- 本批不改背包解析、筛选条件、排序、物品元数据、赠与逻辑、分解逻辑或 storage key。
- 已经自动验收但尚未人工确认。

## 第 5 批 C 执行记录

日期：2026-05-16

目标：统一骰子商店、抽卡设置、自定义物品编辑与碎片商城的关键组件语言，优先修复移动端按钮尺寸、底部动作区和专题弹窗头部一致性。

已完成：

- 骰子商店、物品详情、卡池命名、自定义物品编辑器、碎片商城关闭按钮补齐 `type="button"` 与 `aria-label`。
- 最近收获列表移除内联省略样式，改用 `.acu-gacha-recent-reward-text` 与 `.acu-gacha-recent-quality`。
- 抽卡物品详情的静态字段行移除内联 `cursor`，改用 `.acu-gacha-static-field-row`。
- 设置筛选空状态从内联 `display:none` 改为 `hidden` 属性，并由筛选逻辑同步显隐。
- 移除碎片商城购买和详情点击的调试 `console.warn`。
- 抽卡头部、设置头部、碎片商城头部的图标按钮统一到 `34px` 触控框。
- 移动端单抽/十连按钮改为等宽双列，避免主按钮宽度参差。
- 骰子商城设置底部操作区改为稳定网格，桌面自适应同排，移动端 2 列等宽。
- 碎片商城兑换价格按钮高度提升到 `30px`，减少点按困难。
- 移动端碎片商城头部改为标题左侧、帮助/关闭右侧的同一行布局。

验证：

- 使用 Chrome headless 渲染抽卡主界面、设置弹窗、物品编辑器、碎片商城的桌面和移动检查页。
- 移动端抽卡主界面根容器 `472x673`，无横向溢出，单抽/十连为等宽 `222x46`。
- 移动端设置弹窗根容器 `472x793`，无横向溢出，底部六个按钮为 2 列等宽 `220x40`。
- 移动端物品编辑器根容器 `472x793`，无横向溢出，取消/保存为等宽 `219x40`。
- 移动端碎片商城根容器 `444x528`，无横向溢出，稀有度按钮为 `133x42`，兑换按钮为 `51x30`。
- Chrome DOM 指标复查：四个视图在桌面和移动宽度下 `badButtons=0`，`bodyOverflowX=false`，`rootOverflowX=false`。

验收重点：

- 从仪表盘进入骰子商店，顶部设置、物品栏、打开表格、关闭是否仍可用。
- 单抽和十连是否仍按原逻辑消耗骰运并写入奖励。
- 最近收获条目点击是否仍能打开物品详情。
- 骰子商城设置中卡池管理、筛选、搜索、排序、启用开关和底部新建/导入/导出/清空/返回是否仍可用。
- 自定义物品编辑器的名称、品质、目标、图标、自定义字段、保存/取消是否仍可用。
- 碎片商城卡池切换、稀有度切换、兑换、详情、关闭是否仍可用。

注意：

- 本批不改抽卡概率、保底、钱包、碎片兑换成本、自定义物品 schema、卡池配置、导入导出格式或 storage key。
- 已经自动验收但尚未人工确认。

## 第 5 批 D 执行记录

日期：2026-05-16

目标：统一地图可视化与人物关系图的壳层控件、移动端头部排布和辅助交互语义，让两个图形专题界面不再像独立风格的旧弹窗。

已完成：

- 地图弹窗的回到当前地点、关闭按钮补齐 `type="button"`、`title` 与 `aria-label`。
- 地图地区标签补齐 `type="button"` 与 `aria-pressed`，缩略图补齐 `role="button"`、`tabindex`、`aria-pressed` 与键盘 Enter/Space 选择。
- 地图加载占位移除内联 `grid-column`，改用 `.acu-map-loading-wide`。
- 地图头部动作按钮从 28px 提升到统一的 34px 触控框，移除局部强制 `!important` 尺寸覆盖。
- 地图地区标签在窄屏改为独立横向标签行，标题与关闭路径保持同一视觉节奏。
- 人物关系图的过滤、移动模式、重新布局、管理头像、关闭、重置、节点步进按钮补齐 `type="button"` 与可访问标签。
- 人物关系图中心角色下拉补齐 `aria-expanded`、`role="listbox"`、`role="option"` 与 `aria-selected` 同步。
- 人物关系图底部“节点:”标签移除内联样式，改用 `.acu-graph-node-size-label`。
- 人物关系图移动端头部改为两行稳定布局：中心角色和过滤按钮一行，教程/重排/头像/关闭一行，避免挤压。

验证：

- 使用 Chrome headless 渲染地图和人物关系图检查页，分别覆盖桌面 `1280x900` 与移动 `472x820`。
- 地图桌面根容器 `500x451`，移动根容器 `452x530`，均无横向溢出，`badButtons=0`。
- 人物关系图桌面根容器 `900x700`，移动根容器 `440x656`，均无横向溢出，`badButtons=0`。
- Chrome DOM 指标复查：四个检查视图 `bodyOverflowX=false`、`docOverflowX=false`、`rootOverflowX=false`、`rootOutOfViewport=false`。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回 `[]`。

验收重点：

- 从仪表盘打开地图，地区切换、地点缩略图选择、回到当前地点、教程和关闭是否仍可用。
- 手机宽度下地图标题、地区标签、帮助、回到当前地点、关闭是否不重叠。
- 从仪表盘打开人物关系图，中心角色下拉、在场过滤、直接关系过滤、移动模式、重新布局、角色头像预设、重置视图和关闭是否仍可用。
- 手机宽度下人物关系图头部两行动作是否整齐，底部节点大小控制是否不挤压画布。

注意：

- 本批不改地图数据读取、当前地点缓存、地区排序、关系图解析、力导向布局、节点位置缓存、头像数据或 storage key。
- 已经自动验收但尚未人工确认。

## 第 5 批 E 执行记录

日期：2026-05-16

目标：统一角色头像预设管理器与头像裁剪弹窗的控件语义、触控尺寸、移动端底部动作区和键盘可达性，降低头像专题界面与其他弹窗的割裂感。

已完成：

- 角色头像预设管理器与头像裁剪弹窗补齐 `role="dialog"`、`aria-modal` 与标题关联。
- 角色头像预设管理器的导入、导出、关闭、视图切换、排序方向、编辑、删除、清除头像、清空别名、保存等按钮补齐 `type="button"` 与 `aria-label`。
- 搜索框补齐 `aria-label`，搜索清空从裸图标与内联隐藏改为按钮 + `hidden` 属性控制。
- 头像预览补齐 `role="button"`、`tabindex`、`aria-label`，支持 Enter/Space 打开上传或裁剪。
- 本地上传入口补齐 `role="button"`、`tabindex`、`aria-label`，移除文件输入的内联 `display:none`。
- “主角合并”开关补齐 `aria-pressed`，切换时同步状态。
- 折叠/展开编辑按钮会同步更新 `title` 与 `aria-label`。
- 角色头像预设管理器头部按钮、列表动作按钮、导入导出按钮、搜索清空和主角合并按钮统一到更稳定的触控框。
- 展开区域移除 `max-height` 布局属性动画，改为 `display` 切换配合 `opacity` / `transform`，避免裁剪内容。
- 移动端展开底部动作改为 `34px / 34px / 1fr / 1fr` 网格，清除、别名、本地上传、保存不再挤压。
- 头像裁剪弹窗底部改为图标重传 + 取消 + 确定的稳定三列布局，关闭按钮统一为 34px 图标按钮。

验证：

- 使用 Chrome headless 渲染角色头像预设管理器与头像裁剪弹窗检查页，分别覆盖桌面 `1280x900` 与移动 `472x820`。
- 角色头像预设管理器桌面根容器 `500x491`，移动根容器 `440x491`，均无横向溢出，`badButtons=0`。
- 头像裁剪弹窗桌面和移动根容器均为 `360x394`，无横向溢出，`badButtons=0`。
- Chrome DOM 指标复查：四个检查视图 `bodyOverflowX=false`、`docOverflowX=false`、`rootOverflowX=false`、`rootOutOfViewport=false`。
- `npx --yes impeccable detect --json --fast src/骰子系统` 返回 `[]`。

验收重点：

- 从关系图、仪表盘或“设置 → 骰子系统预设 → 角色头像预设”打开管理器，教程、导入、导出、关闭是否仍可用。
- 视图切换、排序字段、排序方向、搜索与清空搜索是否仍可用。
- 展开角色后，URL、别名、本地上传、清除头像、清空别名、保存是否仍可用。
- 点击头像预览是否仍能在有头像时进入裁剪、无头像时触发上传。
- 头像裁剪弹窗中重新上传、取消、确定、关闭是否仍可用。

注意：

- 本批不改头像 IndexedDB、头像配置结构、导入导出格式、别名解析、删除角色逻辑或 storage key。

## 第 5 批 E 补充记录：角色头像预设入口

日期：2026-05-18

目标：将原“头像管理”运行时入口与教程文案统一为“角色头像预设”，并把入口纳入“设置 → 骰子系统预设”。

已完成：

- “骰子系统预设”列表新增“角色头像预设”管理入口。
- 关系图、仪表盘、设置页、教程提示和备份还原说明中的运行时文案统一为“角色头像预设”。
- 设置页入口默认打开全局头像库；没有当前聊天角色时仍可管理已保存的头像、别名、裁剪和角色颜色。
- 角色头像预设管理器 overlay 与主体补齐 `inset`、`box-sizing`、`100dvh` 最大高度和移动端边界约束，避免弹窗飞出屏幕。

验证：

- 需要覆盖桌面 `1280x900` 与移动 `472x820`，检查设置入口打开后的角色头像预设管理器不超出视口。
- 需要覆盖 `native`、`modern`、`dark` 三个主题，检查标题、工具栏、搜索、列表、关闭路径和弹窗背景可读。
- 需要确认运行时 DOM 文本保持中文，无 `??`、`????` 或 mojibake。

验收重点：

- 从“设置 → 骰子系统预设 → 角色头像预设”打开管理器，默认进入全局头像库。
- 从关系图或仪表盘打开管理器，仍保持当前聊天角色优先。
- 管理器切换当前聊天/全局头像库、排序、搜索、导入、导出、关闭和裁剪弹窗仍可用。
- 已经自动验收但尚未人工确认。

## 第 5 批 F 执行记录

日期：2026-05-16

目标：整理配置备份与还原弹窗的旧式内联样式，让导出态、恢复态和底部动作区使用统一 CSS 类，同时保持备份格式、恢复逻辑和存储 key 不变。

已完成：

- 配置备份模块行、空状态、警告提示、摘要卡片、头部、正文、底部按钮从内联样式迁移到 `.acu-config-backup-*` 样式类。
- 弹窗内联 `<style>` 移除，滚动隐藏、触控滚动和弹窗布局统一进入主样式表。
- 导出 / 确认主按钮统一使用 `.acu-config-backup-primary-btn`，避免重复硬编码强调色和文字色。
- 确认按钮从内联 `display:none` 改为 `hidden` 属性控制，导入成功后通过 `hidden` 同步导出 / 确认按钮显隐。
- 关闭按钮补齐 `title` 与 `aria-label`，并统一到弹窗内 30px 图标按钮。
- 移动端弹窗改为视口内铺展，底部动作区改为分行与双列按钮，恢复摘要卡片移动端自动堆叠。

验证：

- `pnpm build` 通过；骰子系统 bundle 体积警告仍为既有 webpack performance warning。
- `npx impeccable detect --json --fast src/骰子系统` 返回 `[]`。
- `git diff --check` 通过。
- 使用 Chrome DevTools Protocol 渲染配置备份弹窗最小检查页，覆盖桌面 `1024x768` 与移动 `393x852`。
- 导出态与恢复态均无横向溢出，弹窗保持在视口内，底部按钮无重叠。
- 恢复摘要卡片桌面端为 1 行 3 列，移动端为 3 行堆叠。
- 检查页使用 UTF-8 与 viewport meta，DOM 文本未出现 `??` / `????` 编码坏点。

验收重点：

- 从设置打开“备份与还原”，导入、导出、取消、关闭是否仍可用。
- 勾选模块、全选、反选、清空选择是否仍按原逻辑工作。
- 导入备份后，导出按钮是否隐藏，确认按钮是否出现，重新导入是否仍可用。
- 移动端恢复态下摘要、警告、模块列表和底部按钮是否不重叠、不超出视口。

注意：

- 本批不改配置备份格式、schemaVersion、localStorage 键、导入导出文件结构、恢复合并策略或数据库模板导入逻辑。
- 已经自动验收但尚未人工确认。

## 第 5 批 G 执行记录

日期：2026-05-16

目标：收口图标预设管理器与设置入口按钮，让这个专题管理界面的按钮语义、隐藏文件输入和移动端布局继续服从统一控件规则。

已完成：

- 图标预设管理器关闭、导入、导出、保存 URL、上传、保存本地、清空输入、删除按钮补齐 `type="button"`、`title` 或 `aria-label`。
- 图标预设列表条目补齐更明确的 `aria-label`，保留原有 `role="button"` 与键盘 Enter/Space 选择逻辑。
- 隐藏文件输入从内联 `display:none` 改为 `.acu-custom-icon-file-input` 类。
- 空状态说明、缺失提示、列表条目信息和名称布局从内联样式迁移到专用类。
- 高级设置中的紧凑入口按钮统一为 `.acu-settings-compact-action`，移除“管理 / 检验 / 打开 / 清空”按钮重复内联尺寸样式，并补齐 `type="button"`。

验证：

- `pnpm build` 通过；骰子系统 bundle 体积警告仍为既有 webpack performance warning。
- `npx impeccable detect --json --fast src/骰子系统` 返回 `[]`。
- `git diff --check` 通过。
- 源码结构检查确认图标预设管理器目标按钮、隐藏文件输入类和专用样式类均已命中，旧内联样式不再出现在该管理器渲染块中。
- 使用 Chrome DevTools Protocol 渲染图标预设管理器最小检查页，覆盖桌面 `1280x900` 与移动 `393x852`。
- 桌面和移动下均无横向溢出，弹窗保持在视口内；8 个目标按钮 `type="button"` 与 `aria-label` 均有效，按钮文字无溢出。
- 两个文件输入 `display:none` 生效；检查页使用 UTF-8 与 viewport meta，DOM 文本未出现 `??` / `????` 编码坏点。

验收重点：

- 从“设置 → 骰子系统预设”使用并进入“图标预设管理”，教程、关闭、导入图标包、导出图标包是否仍可用。
- 模块过滤、表格过滤、搜索是否仍能筛选左侧条目。
- 点击或键盘选择左侧条目后，右侧详情是否正常刷新。
- 保存 URL、上传本地图片、保存本地、清空输入、删除映射是否仍按原逻辑工作。
- 移动端下工具栏、列表、详情区和底部操作按钮是否不重叠、不超出视口。

注意：

- 本批不改图标预设白名单、IndexedDB 图片存储、导入导出格式、URL 校验、映射 key、刷新消费者逻辑或 storage key。
- 已经自动验收但尚未人工确认。

## 第 5 批 H 执行记录

日期：2026-05-17

目标：统一变量过滤黑名单管理弹窗，让这个高级设置子面板从旧式内联样式迁移到共享弹窗、表单与底部动作组件，同时保持黑名单解析和应用逻辑不变。

已完成：

- 黑名单弹窗遮罩改用 `.acu-edit-overlay acu-blacklist-manager-overlay`，弹窗补齐 `role="dialog"`、`aria-modal` 与标题关联。
- 标题栏改为专用头部类，关闭按钮补齐 `type="button"`、`title` 与 `aria-label`。
- 说明文本、textarea、单项输入行、添加按钮和底部动作区从内联样式迁移到 `.acu-blacklist-*` 样式类。
- textarea 与单项输入补齐 `aria-label`，textarea 关联说明文本。
- 添加、重置、完成按钮统一使用 `acu-dialog-btn` / `acu-btn-confirm` 组件语法，并补齐 `type`、`title`、`aria-label`。
- “重置为默认”二次确认从浏览器原生 `confirm()` 改为骰子系统内确认弹窗。
- 全量清理骰子系统内剩余浏览器原生 `confirm()` / `window.confirm()`，改为通用 `showDiceSystemConfirmDialog`，覆盖清理历史、删除收藏、删除/恢复预设、删除规则、旧版本预设合并等确认路径。
- 通用确认弹窗补齐 `acu-system-confirm-*` 语义类，继续复用既有系统确认弹窗视觉。
- 移除运行时 `$modal.css(...)` 写入遮罩布局，改由主样式表控制弹窗尺寸、z-index 和响应式约束。

验证：

- `pnpm build` 通过；骰子系统 bundle 体积警告仍为既有 webpack performance warning。
- `npx impeccable detect --json --fast src/骰子系统` 返回 `[]`。
- `git diff --check` 通过。
- `rg -n "\b(?:window\.)?confirm\s*\(" src/骰子系统/index.ts src/骰子系统/styles.ts src/骰子系统/tutorial.ts` 无命中。
- 使用 Chrome DevTools Protocol 渲染黑名单管理弹窗最小检查页，覆盖桌面 `1280x900` 与移动 `393x852`。
- 桌面弹窗 `600x352`，移动弹窗 `354x367`，均无横向溢出且未超出视口。
- 检查页确认 4 个按钮均为 `type="button"` 且有 `title` / `aria-label`，中文文本未出现 `??` / `????` 编码坏点。
- 追加检查“重置为默认”确认弹窗叠加态：桌面确认弹窗 `420x360`，移动确认弹窗 `361x355`，均无横向溢出、未超出视口，并位于黑名单弹窗上层。
- 追加检查通用 `acu-system-confirm-*` 确认弹窗：桌面 `420x341`，移动 `369x336`，均无横向溢出、未超出视口，z-index 为 `32100`。

验收重点：

- 从高级设置打开“过滤黑名单”，关闭按钮、完成按钮和点击遮罩关闭是否仍会刷新界面并应用变化。
- textarea 中使用中英文逗号编辑黑名单是否仍会即时去重并同步。
- 单项输入框输入一个或多个关键词后点击“添加”，以及按 Enter 添加，是否仍可用。
- 重置为默认是否弹出骰子系统自己的确认弹窗；确认后恢复默认黑名单，取消后不修改当前列表。
- 检定历史清理、收藏删除、属性/检定/交互/仪表盘预设删除、数据验证与正则规则删除、默认预设恢复等路径是否都弹出骰子系统确认弹窗。
- 移动端下标题、输入框、添加按钮、重置和完成按钮是否不重叠、不超出视口。

注意：

- 本批不改默认黑名单内容、逗号分隔规则、去重逻辑、`acu_filter_blacklist_v1` / `acu_filter_blacklist_version` storage key 或快捷骰子图标过滤逻辑。
- 已经自动验收但尚未人工确认。

## 第 5 批 I 执行记录

日期：2026-05-17

目标：继续收口浏览器原生弹窗家族，把常规 UI 路径中的 `prompt()` / `alert()` 迁移到骰子系统自己的输入、确认或错误提示组件。

已完成：

- 新增通用 `showDiceSystemInputDialog`，复用系统弹窗壳层，用于命名输入、数量输入和手动复制文本。
- 数据验证预设新建、表格正则预设新建、表格正则预设复制从浏览器原生 `prompt()` 改为系统输入弹窗。
- 拆解物品数量输入从 `window.prompt()` 改为系统输入弹窗，保留原有数量解析和合法性检查。
- 复制失败的手动复制兜底从原生 `prompt()` 改为只读多行系统输入弹窗。
- 保存失败、手动更新失败、手动填表失败、数据结构异常等常规 UI 错误兜底从原生 `alert()` 改为系统弹窗或现有错误 toast。
- `DESIGN.md` 增补原生 `prompt()` / `alert()` 禁用规则，要求命名、数量输入、手动复制等路径复用系统输入弹窗。
- 初始化失败紧急入口保留 2 处 `alert()` 作为系统 UI 尚未初始化时的浏览器级兜底，并在代码旁注明例外原因。

验证：

- `pnpm build` 通过；骰子系统 bundle 体积警告仍为既有 webpack performance warning。
- `npx impeccable detect --json --fast src/骰子系统` 返回 `[]`。
- `git diff --check` 通过。
- `rg -n "\b(?:window\.)?prompt\s*\(" src/骰子系统/index.ts src/骰子系统/styles.ts src/骰子系统/tutorial.ts` 无命中。
- `rg -n "\b(?:window\.)?alert\s*\(" src/骰子系统/index.ts src/骰子系统/styles.ts src/骰子系统/tutorial.ts` 仅剩初始化失败紧急入口 2 处注释例外。
- 使用 Chrome DevTools Protocol 渲染系统输入弹窗最小检查页，覆盖桌面 `1280x900` 与移动 `393x852`。
- 桌面输入弹窗 `460x265`，移动输入弹窗 `361x265`，均无横向溢出、未超出视口，中文文本未出现 `??` / `????` 编码坏点。

验收重点：

- 数据验证预设新建、表格正则预设新建、表格正则预设复制是否弹出骰子系统输入弹窗，确认与取消路径是否仍正确。
- 物品详情中拆解多数量物品时，数量输入是否弹出系统输入弹窗，非法数量提示是否仍可用。
- 复制失败时的手动复制兜底是否显示系统多行只读输入弹窗。
- 保存失败、手动更新失败、手动填表失败等错误兜底是否不再弹浏览器原生 alert。
- 脚本初始化失败时的紧急入口仍可在系统 UI 不可用时提示打开浏览器控制台。

注意：

- 本批不改预设创建、复制、导入、拆解、保存、复制文本或错误处理的数据逻辑，只替换浏览器原生弹窗呈现层。
- 已经自动验收但尚未人工确认。

## 第 5 批 J 执行记录

日期：2026-05-17

目标：修复骰子商店移动端物品详情弹窗的关闭按钮排布，让移动端关闭路径回到统一的头部图标按钮语义。

已完成：

- 移动端抽卡物品详情头部改为标题信息与关闭按钮同一行两列网格，避免关闭按钮单独掉到下一行形成多余按钮框感。
- 移动端抽卡物品详情关闭按钮收敛为 `34px` 透明图标按钮；桌面端保留既有表现不变。
- 修复范围限定在 `.acu-gacha-pickup-detail` 的移动端样式，不影响普通物品详情、碎片商城和桌面骰子商店。

验证：

- `pnpm build` 通过；骰子系统 bundle 体积警告仍为既有 webpack performance warning。
- `npx impeccable detect --json --fast src/骰子系统` 返回 `[]`。
- `git diff --check` 通过。
- 使用 Chrome DevTools Protocol 渲染骰子商店物品详情最小检查页，覆盖桌面 `1024x768` 与移动 `393x852`。
- 移动端详情弹窗 `369x293`，关闭按钮 `34x34`、透明背景、透明边框，与标题主区域顶部差值为 `0`，无横向溢出且中文文本无编码坏点。
- 桌面端关闭按钮仍为既有 `40x40`，确认本次修复没有改动 PC 表现。

验收重点：

- 移动端骰子商店中点击奖品详情时，关闭按钮是否回到弹窗头部右侧，不再独占一行或出现突兀框感。
- PC 版骰子商店物品详情关闭按钮是否保持原来的可接受表现。

注意：

- 本批只修复移动端抽卡物品详情弹窗布局与关闭按钮视觉，不改抽卡、发放、兑换或物品详情数据逻辑。
- 已经自动验收但尚未人工确认。

## 第 5 批 K 执行记录

日期：2026-05-17

目标：继续清理专题弹窗里的旧式内联关闭按钮，让普通表格卡片预览也服从统一图标按钮语义。

已完成：

- 普通表格卡片预览关闭按钮移除内联 `background:none;border:none;padding:4px` 样式，改为 `acu-card-preview-close` 语义类。
- 关闭按钮补齐 `type="button"`、`title` 和 `aria-label`，保留原有 `.acu-preview-close` 事件钩子，关闭逻辑不变。
- 新增 `.acu-preview-overlay .acu-card-preview-close` 样式，统一为 `34px` 透明图标按钮，并提供 hover / focus-visible 状态。

验证：

- 使用 Chrome DevTools Protocol 渲染普通表格卡片预览最小检查页，覆盖桌面 `1024x768` 与移动 `393x852`。
- 桌面和移动下关闭按钮均为 `34x34`，无内联样式，透明背景、透明边框，标题位于关闭按钮左侧且无横向溢出。
- 中文文本未出现 `??` / `????` 编码坏点。

验收重点：

- 普通表格卡片预览弹窗右上角关闭按钮是否不再像单独写死的小按钮。
- 移动端卡片标题和关闭按钮是否保持同一行，不互相挤压。

注意：

- 本批只调整普通表格卡片预览关闭按钮的语义和视觉，不改卡片预览内容、编辑、交互选项或关闭事件逻辑。
- 已经自动验收但尚未人工确认。

## 第 5 批 L 执行记录

日期：2026-05-17

目标：统一“发送消息给某人”弹窗，清理交谈动作和仪表盘发消息入口中残留的整段内联弹窗样式。

已完成：

- 交互动作“交谈”和仪表盘重要人物“发消息”共用 `acu-msg-*` 弹窗类，移除 overlay、dialog、标题、输入框和按钮上的内联视觉样式。
- 发送消息弹窗补齐 `role="dialog"`、`aria-modal`、`aria-label`，按钮补齐 `type="button"`。
- 输入框、取消按钮、发送按钮迁移到统一 CSS；保留原有回车发送、取消、点击背景关闭和写入输入框逻辑。

验证：

- 使用 Chrome DevTools Protocol 渲染发送消息弹窗最小检查页，覆盖桌面 `1024x768` 与移动 `393x852`。
- 桌面和移动下弹窗宽度 `320px`，输入框与两个底部按钮高度均为 `38px`，关键节点无内联样式，无横向溢出。
- 中文文本未出现 `??` / `????` 编码坏点。

验收重点：

- 从普通交互动作点击“交谈”时，输入消息、回车发送、取消和点击背景关闭是否仍正常。
- 从仪表盘重要人物点击发消息时，插入的 `<user>对某人说` 文本是否仍正常。
- 移动端发送消息弹窗是否不超出视口，底部两个按钮是否等宽。

注意：

- 本批只调整发送消息弹窗的语义和视觉，不改交互动作模板、消息文本格式或发送后聚焦逻辑。
- 已经自动验收但尚未人工确认。
